name: Build Linux x86_64 libraries.

on: [push]

jobs:
  build_linux_x86_64:
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v6
        with:
          submodules: 'recursive'

      - name: autogen
        run: cd libusb && ./autogen.sh --disable-udev

      - name: make
        run: cd libusb && make && cp libusb/.libs/libusb-1.0.so libusb/.libs/libusb-linux-x86_64-1.0.29.so

      - uses: actions/upload-artifact@v7
        with:
          name: libusb-linux-x86_64-1.0.29.so
          path: libusb/libusb/.libs/libusb-linux-x86_64-1.0.29.so
